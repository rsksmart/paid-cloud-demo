{
  "name": "@paid-cloud/contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "build": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy": "npx hardhat run --network localhost scripts/deploy.ts",
    "pay": "npx hardhat run --network localhost scripts/payRandomPeriod.ts",
    "transfer": "npx hardhat run --network localhost scripts/transferTo.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.11.2"
  }
}
